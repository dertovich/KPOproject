#include "libft.h"

/*
** LIBRARY: N/A
** DESCRIPTION:
**		The ft_isnegative() function checks if a number is negative.
*/

int	ft_isnegative(int n)
{
	if (n < 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
